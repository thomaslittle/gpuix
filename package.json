{
  "name": "gpuix",
  "private": true,
  "workspaces": [
    "packages/*",
    "examples"
  ],
  "scripts": {
    "build": "bun run build:native && bun run build:react",
    "build:native": "cd packages/native && bun run build",
    "build:react": "cd packages/react && bun run build",
    "example:counter": "cd examples && bun --hot counter.tsx",
    "dev": "bun scripts/dev.ts",
    "windows:qualify": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/windows-runtime-qualification.ps1",
    "windows:qualify:runtime": "bun scripts/windows-runtime-qualification.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-reconciler": "^0.33.0"
  }
}
